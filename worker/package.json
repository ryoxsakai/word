{
  "name": "vocab-app-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "test:mcp-write": "node test/access-auth.test.mjs && node test/mcp-write.integration.mjs",
    "db:migrate:local": "wrangler d1 migrations apply vocab-db --local",
    "db:migrate:remote": "wrangler d1 migrations apply vocab-db --remote",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "miniflare": "^4.20260701.0",
    "wrangler": "^4.0.0"
  }
}
